services:
  - type: web
    name: isps-datasette
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: datasette isps.db --host 0.0.0.0 --port $PORT -m metadata.json --setting sql_time_limit_ms 10000
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
